/* FSDC Meatfest — physical buffet allocation + screen/print presentation. */
(() => {
  const B0=window.BuffetEngine;if(!B0||typeof B0.plan!=='function')return;
  const stationOrder=['entry','cold','vegetable','starch','core','specialty','bread','finish'];
  const rank=k=>{const i=stationOrder.indexOf(k);return i<0?99:i};
  const itemName=x=>x?.name||x?.side?.name||x?.bread?.name||x?.item?.name||x?.id||'Service item';
  const preferred=g=>{const items=g?.items||[];if(items.some(x=>x.id==='sauerkraut'))return 2;const s=g?.station;return ({entry:0,cold:0,vegetable:1,starch:1,core:2,specialty:2,bread:3,finish:3}[s]??3)};
  function width(g){const v=g?.items?.[0]?.vessel?.type;if(v==='jar')return 4;return Math.max(0,Number(g?.linearIn)||0)}
  function allocate(groups,tableLengths=B0.TABLE_GEOMETRY.main){const lengths=tableLengths.map(Number).filter(n=>Number.isFinite(n)&&n>0);const seg=lengths.map((length,i)=>({table:i+1,length,items:[],used:0,remaining:length,stations:[],overflow:false}));const overflow=[];for(const g of groups||[]){const w=width(g);if(!w)continue;let placed=false;const start=Math.min(preferred(g),seg.length-1);for(let i=start;i<seg.length;i++){if(w<=seg[i].remaining+1e-9){seg[i].items.push(g);seg[i].used+=w;seg[i].remaining-=w;if(!seg[i].stations.includes(g.station))seg[i].stations.push(g.station);placed=true;break}}if(!placed)overflow.push(g)}const required=(groups||[]).reduce((s,g)=>s+width(g),0),provided=lengths.reduce((s,n)=>s+n,0),overflowIn=Math.max(0,required-provided);const overflowItems=overflow.flatMap(g=>(g.items||[]).map(itemName));let recommended=[];try{recommended=B0.tableRequirement(groups?.map(g=>({...g,linearIn:width(g)})),{tableLengths:[72,48]}).tables}catch{}return{shape:'U',tableLengths:lengths,linearRequired:required,linearProvided:provided,overflow:overflow.length>0||required>provided,overflowIn,overflowGroups:overflow,overflowItems,overflowStations:[...new Set(overflow.map(g=>g.station))].sort((a,b)=>rank(a)-rank(b)),segments:seg,recommendedTables:recommended}}
  function plan(input={}){const p=B0.plan(input),layout=allocate(p.serviceGroups,input.mainTableLengths||B0.TABLE_GEOMETRY.main);return{...p,tables:{...p.tables,tables:layout.tableLengths,linearRequired:layout.linearRequired,linearProvided:layout.linearProvided,layout,overflow:layout.overflow}}}
  const B=Object.freeze({...B0,plan});window.BuffetEngine=B;window.BuffetAllocation=Object.freeze({allocate,itemName,preferred});
  const STYLE_ID='buffetLayoutVisualStyle';let obs=null;const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
  function selectedIds(key){return [...document.querySelectorAll(`[data-buffet-key="${key}"].on`)].map(x=>x.dataset.buffetId).filter(Boolean)}
  function currentPlan(){if(!window.buildSummary)return null;const s=window.buildSummary(),sausage=document.querySelector('[data-buffet-sausage].on')?.dataset.buffetSausage||'polish',load=document.getElementById('buffetDessertLoad')?.value||'moderate';return B.plan({proteinKeys:s.rows.map(r=>r.key),sideIds:s.sideRows.map(r=>r.id),sideRows:s.sideRows,eaters:s.eaters,breadIds:selectedIds('breadIds'),supplementalIds:selectedIds('supplementalIds'),condimentIds:selectedIds('condimentIds'),dessertIds:selectedIds('dessertIds'),load,sausageMode:sausage})}
  function style(){if(document.getElementById(STYLE_ID))return;const s=document.createElement('style');s.id=STYLE_ID;s.textContent=`#buffetLayoutCard .visualLayout{margin-top:14px;border:1px solid #30353b;border-radius:14px;background:#101214;padding:14px}#buffetLayoutCard .visualLayoutHead{display:flex;justify-content:space-between;gap:12px;align-items:flex-end;margin-bottom:12px}#buffetLayoutCard .visualLayoutTitle{font-size:11px;font-weight:900;letter-spacing:.12em;color:#c9cdd2}#buffetLayoutCard .visualLayoutMeta{font-size:10px;color:#aeb3b9;text-align:right}#buffetLayoutCard .uMap{display:grid;grid-template-columns:74px 1fr 74px;grid-template-rows:92px 92px 58px;gap:8px;align-items:stretch}#buffetLayoutCard .uTable{border:2px solid #575d64;border-radius:9px;background:#20242a;display:flex;flex-direction:column;justify-content:center;align-items:center;padding:7px;text-align:center;min-width:0;overflow:hidden}#buffetLayoutCard .uTable b{font-size:11px}.uTable small{font-size:8px;color:#aeb3b9;margin-top:3px;line-height:1.25}#buffetLayoutCard .uTable.t1{grid-column:1;grid-row:1 / span 2}.uTable.t2{grid-column:2;grid-row:1}.uTable.t3{grid-column:3;grid-row:1 / span 2}.uTable.t4{grid-column:2;grid-row:2 / span 2}#buffetLayoutCard .uOpen,#buffetLayoutCard .uEntry{display:flex;align-items:center;justify-content:center;color:#626971;font-size:8px;text-transform:uppercase;letter-spacing:.08em}#buffetLayoutCard .uOpen{grid-column:1;grid-row:3}#buffetLayoutCard .uEntry{grid-column:3;grid-row:3}#buffetLayoutCard .uItems{display:flex;flex-wrap:wrap;justify-content:center;gap:3px;margin-top:5px}.uItem{font-size:7px;color:#d9dde1;border:1px solid #3a4047;border-radius:999px;padding:3px 4px;max-width:100%;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}#buffetLayoutCard .visualLegend{display:flex;flex-wrap:wrap;gap:6px;margin-top:10px}.visualLegend span{font-size:8px;color:#aeb3b9;border:1px solid #30353b;border-radius:999px;padding:4px 6px}#buffetLayoutCard .flowStrip{display:flex;gap:5px;overflow:auto;margin-top:12px;padding-bottom:2px}.flowStrip span{flex:0 0 auto;border:1px solid #3a4047;border-radius:999px;padding:5px 7px;font-size:8px;color:#d9dde1}.layoutOverflow{border-color:#8d4b35!important;background:#281b17!important}@media(max-width:600px){#buffetLayoutCard .uMap{grid-template-columns:58px 1fr 58px;grid-template-rows:82px 82px 52px}.uItem{font-size:6px;padding:2px 3px}}
#buffetLayoutCard .allocationNotice{margin-top:10px;padding:9px 10px;border-left:3px solid #f39a32;background:#1d1914;color:#d9c7ae;font-size:10px;line-height:1.4;border-radius:0 7px 7px 0}`;document.head.appendChild(s)}
  function names(seg){const out=[];for(const g of seg.items||[])for(const x of g.items||[]){const n=itemName(x);if(n&&!out.includes(n))out.push(n)}return out}
  function syncBaseLayout(p){const card=document.getElementById('buffetLayoutCard'),wrap=card?.querySelector('.layoutTables');if(!wrap||!p?.tables?.layout)return;const l=p.tables.layout;wrap.innerHTML=l.segments.map(seg=>`<div class="layoutTable ${seg.overflow?'layoutOverflow':''}"><div class="layoutTableTitle"><span>Table ${seg.table}</span><span>${seg.length/12}'</span></div><div class="layoutBar"><div class="layoutFill" style="width:${Math.min(100,Math.max(0,seg.used/seg.length*100))}%"></div></div><div class="layoutItems">${names(seg).map(n=>`<span class="layoutItem">${esc(n)}</span>`).join('')||'<span class="layoutItem">Service space</span>'}</div><div class="layoutStation">${seg.stations.map(k=>B.STATION_LABELS?.[k]||k).join(' • ')||'Service space'} • ${Math.max(0,seg.used)}" used</div></div>`).join('');let n=card.querySelector('.allocationNotice');if(n)n.remove();if(l.overflow){const extra=document.createElement('div');extra.className='allocationNotice';extra.textContent=`OVERFLOW: ${l.overflowIn}" of service space does not fit the four-table main footprint. Recommended table set: ${l.recommendedTables.map(x=>x/12+"'").join(' + ')||'additional service surface'}.`;wrap.after(extra)}}
  function render(){const card=document.getElementById('buffetLayoutCard');if(!card)return;style();if(obs)obs.disconnect();const p=currentPlan();if(p)syncBaseLayout(p);const old=card.querySelector('.visualLayout');if(old)old.remove();const tables=[...card.querySelectorAll('.layoutTable')];if(!tables.length){if(obs)obs.observe(card,{childList:true,subtree:true});return}const data=tables.map(t=>({length:(t.querySelector('.layoutTableTitle span:last-child')?.textContent||'').trim(),items:[...t.querySelectorAll('.layoutItem')].map(x=>x.textContent.trim()),station:(t.querySelector('.layoutStation')?.textContent||'').trim(),overflow:t.classList.contains('layoutOverflow')})),order=data.slice(0,4),table=(d,cls)=>d?`<div class="uTable ${cls} ${d.overflow?'layoutOverflow':''}"><b>Table ${order.indexOf(d)+1} • ${esc(d.length)}</b><small>${esc(d.station||'Service space')}</small><div class="uItems">${d.items.slice(0,10).map(x=>`<span class="uItem">${esc(x)}</span>`).join('')}</div></div>`:'';const flow=[...card.querySelectorAll('.buffetRow')].map(r=>r.querySelector('span')?.textContent?.trim()).filter(Boolean);const v=document.createElement('div');v.className='visualLayout';v.innerHTML=`<div class="visualLayoutHead"><div><div class="visualLayoutTitle">MAIN BUFFET — U-SHAPE</div><div class="note">Physical allocation follows service-vessel space and the established buffet sequence.</div></div><div class="visualLayoutMeta">3 × 6' + 1 × 4'<br>66 sq ft main surface${p?.tables?.overflow?'<br><b>OVER CAPACITY</b>':''}</div></div><div class="uMap">${table(order[0],'t1')}${table(order[1],'t2')}${table(order[2],'t3')}${table(order[3],'t4')}<div class="uOpen">guest approach</div><div class="uEntry">entry / exit</div></div><div class="visualLegend"><span>LEFT: cold + fresh</span><span>CENTER: vegetables + starches</span><span>RIGHT: BBQ proteins</span><span>END: bread + sauces</span></div>${flow.length?`<div class="flowStrip">${flow.slice(0,12).map((x,i)=>`<span>${i+1}. ${esc(x)}</span>`).join('')}</div>`:''}</div>`;card.insertBefore(v,card.firstChild?.nextSibling||null);if(obs)obs.observe(card,{childList:true,subtree:true})}
  const schedule=()=>requestAnimationFrame(render);obs=new MutationObserver(schedule);function start(){const card=document.getElementById('buffetLayoutCard');if(!card){setTimeout(start,100);return}obs.observe(card,{childList:true,subtree:true});render()}start();
})();
