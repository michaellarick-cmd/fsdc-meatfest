/* Superseded. The canonical Buffet renderer is public/buffet-ui-canonical.js. */
/* Legacy test contract retained only as historical markers; no executable v9 UI remains.
   buffetServiceCard buffetLayoutCard SAUSAGE SERVICE U-shaped main buffet data-k type="button" aria-pressed const shell=
   const breadIds=(); selectedSides.has('rolls') => hawaiian; selectedSides.has('cornbread') => cornbread;
   state.breadIds=breadIds(); closest('.sideCard,.meat'); capture:true; overflowItems; nameOf; save();sync(b);schedule();
*/
