const { chromium, webkit } = require('playwright');
const LIVE_URL = process.env.MEATFEST_LIVE_URL || 'https://fsdc-meatfest.michael-larick.workers.dev/';
const BROWSER_NAME = process.env.MEATFEST_BROWSER || 'chromium';
const VIEWPORT_HEIGHT = 844;
const fail = message => { throw new Error(message); };
async function runMobile(browser){const context=await browser.newContext({viewport:{width:390,height:VIEWPORT_HEIGHT},deviceScaleFactor:2,isMobile:true,hasTouch:true});const page=await context.newPage();try{await page.addInitScript(()=>{localStorage.clear();sessionStorage.clear();window.__meatfestWorkerPosts=0;const originalPostMessage=Worker.prototype.postMessage;Worker.prototype.postMessage=function(...args){window.__meatfestWorkerPosts++;return originalPostMessage.apply(this,args)}});const response=await page.goto(LIVE_URL,{waitUntil:'domcontentloaded',timeout:60000});if(!response||!response.ok())fail(`${BROWSER_NAME} Cloudflare page request failed: ${response?response.status():'no response'}`);await page.addStyleTag({content:'html{scroll-behavior:auto !important}'});await page.locator('meatfest-buffet #buffetServiceCard').waitFor({state:'attached',timeout:30000});await page.locator('meatfest-buffet #buffetLayoutCard').waitFor({state:'attached',timeout:30000});await page.locator('meatfest-buffet').scrollIntoViewIfNeeded();await page.waitForFunction(()=>{const root=document.querySelector('meatfest-buffet')?.shadowRoot;const h=root?.querySelector('[data-mf-section="layout"] .table b');return !!h&&!/waiting for plan/.test(h.textContent||'')},{timeout:30000});if(await page.evaluate(()=>window.__meatfestWorkerPosts)<1)fail(`${BROWSER_NAME}: Buffet never performed its initial calculation.`);const button=(kind,label)=>page.locator(`meatfest-buffet button[data-kind="${kind}"]`).filter({hasText:label}).first();const state=()=>page.evaluate(()=>{const root=document.querySelector('meatfest-buffet')?.shadowRoot;return{y:scrollY,h:document.documentElement.scrollHeight,pressed:[...root.querySelectorAll('button[data-kind]')].filter(b=>b.getAttribute('aria-pressed')==='true').map(b=>b.textContent.trim().replace(/^✓\s*/,'')),rows:root?.querySelectorAll('[data-mf-section="service"] .row').length||0,tables:root?.querySelectorAll('[data-mf-section="layout"] .table').length||0,posts:window.__meatfestWorkerPosts}});await button('supplemental','Burgers').tap();await button('supplemental','Grilling Brats').tap();await page.waitForTimeout(350);const afterProteins=await state();if(!afterProteins.pressed.includes('Burgers')||!afterProteins.pressed.includes('Grilling Brats'))fail(`${BROWSER_NAME}: mobile protein selections did not persist before scroll: ${JSON.stringify(afterProteins)}`);await page.evaluate(()=>window.__meatfestWorkerPosts=0);const startY=await page.evaluate(()=>scrollY),trace=[];for(let i=0;i<8;i++){const before=await state();await page.evaluate(()=>window.scrollBy(0,600));await page.waitForTimeout(100);const after=await state();trace.push({before,after});if(after.y<=before.y)break;}const maxY=Math.max(startY,...trace.map(x=>x.after.y)),minHeight=Math.min(...trace.map(x=>x.after.h)),maxHeight=Math.max(...trace.map(x=>x.after.h)),afterScroll=await state();if(trace.some(x=>x.after.y<x.before.y-50))fail(`${BROWSER_NAME}: mobile scroll jumped backward after Buffet interaction: ${JSON.stringify(trace)}`);if(maxY<startY+250)fail(`${BROWSER_NAME}: mobile scroll did not move through Buffet after selections: startY=${startY} maxY=${maxY} trace=${JSON.stringify(trace)}`);if(maxHeight-minHeight>50)fail(`${BROWSER_NAME}: document height changed during mobile scroll: min=${minHeight} max=${maxHeight}`);if(afterScroll.posts>1)fail(`${BROWSER_NAME}: repeated calculation work during mobile scroll: workerPosts=${afterScroll.posts}`);if(!afterScroll.pressed.includes('Burgers')||!afterScroll.pressed.includes('Grilling Brats'))fail(`${BROWSER_NAME}: mobile scroll lost Buffet selections: ${JSON.stringify(afterScroll)}`);if(afterScroll.rows!==15||afterScroll.tables!==8)fail(`${BROWSER_NAME}: Buffet structure changed during mobile scroll: ${JSON.stringify(afterScroll)}`);console.log(`${BROWSER_NAME} mobile Buffet interaction+scroll regression passed.`)}finally{await context.close()}}
async function runDesktop(browser){const page=await browser.newPage({viewport:{width:1280,height:900},deviceScaleFactor:1,isMobile:false});const events=[];page.on('pageerror',error=>events.push(`pageerror: ${error.message}`));page.on('console',msg=>{if(msg.type()==='error')events.push(`console: ${msg.text()}`)});try{await page.addInitScript(()=>{localStorage.clear();sessionStorage.clear();window.__mfTrace={posts:0,connects:0,disconnects:0};const originalPostMessage=Worker.prototype.postMessage;Worker.prototype.postMessage=function(...args){window.__mfTrace.posts++;return originalPostMessage.apply(this,args)};const wrap=proto=>{if(!proto||proto.__mfLifecycleWrapped)return;proto.__mfLifecycleWrapped=true;const originalConnected=proto.connectedCallback,originalDisconnected=proto.disconnectedCallback;proto.connectedCallback=function(){window.__mfTrace.connects++;return originalConnected?.call(this)};proto.disconnectedCallback=function(){window.__mfTrace.disconnects++;return originalDisconnected?.call(this)}};const originalDefine=customElements.define.bind(customElements);customElements.define=function(name,ctor,options){if(name==='meatfest-buffet')wrap(ctor.prototype);return originalDefine(name,ctor,options)}});const response=await page.goto(LIVE_URL,{waitUntil:'domcontentloaded',timeout:60000});if(!response||!response.ok())fail(`desktop Cloudflare page request failed: ${response?response.status():'no response'}`);await page.addStyleTag({content:'html{scroll-behavior:auto !important}'});await page.locator('meatfest-buffet #buffetServiceCard').waitFor({state:'attached',timeout:30000});await page.locator('meatfest-buffet #buffetLayoutCard').waitFor({state:'attached',timeout:30000});await page.waitForFunction(()=>{const root=document.querySelector('meatfest-buffet')?.shadowRoot;const h=root?.querySelector('[data-mf-section="layout"] .table b');return !!h&&!/waiting for plan/.test(h.textContent||'')},{timeout:30000});const initial=await page.evaluate(()=>{const el=document.querySelector('meatfest-buffet'),root=el.shadowRoot;return{y:scrollY,h:document.documentElement.scrollHeight,version:window.MeatfestBuffet?.version,app:performance.getEntriesByType('resource').filter(x=>x.name.includes('buffet-app.js')||x.name.includes('buffet-ui.js')).map(x=>x.name),host:el.isConnected,pressed:[...root.querySelectorAll('button[data-kind]')].filter(b=>b.dataset.kind!=='sausage'&&b.getAttribute('aria-pressed')==='true').map(b=>b.textContent.trim().replace(/^✓\s*/,'')),trace:window.__mfTrace}});if(initial.version!==8)fail(`desktop: wrong Buffet version ${initial.version}`);if(!initial.app.some(x=>x.includes('buffet-app.js?v=7')))fail(`desktop: buffet-app.js?v=7 was not loaded: ${JSON.stringify(initial.app)}`);if(initial.pressed.length)fail(`desktop: clean profile still has persisted Buffet selections: ${JSON.stringify(initial.pressed)}`);const button=(kind,label)=>page.locator(`meatfest-buffet button[data-kind="${kind}"]`).filter({hasText:label}).first();const state=()=>page.evaluate(()=>{const host=document.querySelector('meatfest-buffet'),root=host?.shadowRoot;return{y:scrollY,h:document.documentElement.scrollHeight,hostConnected:!!host?.isConnected,pressed:[...root.querySelectorAll('button[data-kind]')].filter(b=>b.getAttribute('aria-pressed')==='true').map(b=>b.textContent.trim().replace(/^✓\s*/,'')),rows:root?.querySelectorAll('[data-mf-section="service"] .row').length||0,tables:root?.querySelectorAll('[data-mf-section="layout"] .table').length||0,trace:{...window.__mfTrace}}});await page.evaluate(()=>window.scrollTo(0,Math.max(0,document.documentElement.scrollHeight*.35)));await page.waitForTimeout(100);await button('supplemental','Burgers').click();await button('supplemental','Grilling Brats').click();const afterProteins=await state();if(!afterProteins.pressed.includes('Burgers')||!afterProteins.pressed.includes('Grilling Brats'))fail(`desktop: protein selections did not persist: ${JSON.stringify(afterProteins)}`);await page.mouse.wheel(0,900);await page.waitForTimeout(150);const beforeCondiment=await state();await button('condiment','BBQ Sauce').click();const afterCondiment=await state();if(!afterCondiment.pressed.includes('Burgers')||!afterCondiment.pressed.includes('Grilling Brats')||!afterCondiment.pressed.includes('BBQ Sauce'))fail(`desktop: condiment selection reset earlier choices: ${JSON.stringify({beforeCondiment,afterCondiment})}`);await page.mouse.wheel(0,1400);await page.waitForTimeout(150);await button('dessert','Cobbler / Crisp').click();const afterDessert=await state();if(!afterDessert.pressed.includes('Burgers')||!afterDessert.pressed.includes('Grilling Brats')||!afterDessert.pressed.includes('BBQ Sauce')||!afterDessert.pressed.includes('Cobbler / Crisp'))fail(`desktop: dessert selection reset earlier choices: ${JSON.stringify({beforeCondiment,afterDessert})}`);const yAfterDessert=afterDessert.y;await page.mouse.wheel(0,-1200);await page.waitForTimeout(100);await page.mouse.wheel(0,700);await page.waitForTimeout(100);const final=await state();if(final.y===yAfterDessert)fail('desktop: scroll did not move after interaction');if(final.h!==afterDessert.h)fail(`desktop: document height changed after selection/scroll: before=${afterDessert.h} after=${final.h}`);if(final.rows!==15||final.tables!==8)fail(`desktop: structure changed: ${JSON.stringify(final)}`);if(!final.pressed.includes('Burgers')||!final.pressed.includes('Grilling Brats')||!final.pressed.includes('BBQ Sauce')||!final.pressed.includes('Cobbler / Crisp'))fail(`desktop: selections were lost after scroll-away/back: ${JSON.stringify(final)}`);if(final.trace.connects!==initial.trace.connects||final.trace.disconnects!==initial.trace.disconnects)fail(`desktop: Buffet custom element lifecycle changed during interaction: initial=${JSON.stringify(initial.trace)} final=${JSON.stringify(final.trace)}`);if(events.length)fail(`desktop: browser errors observed: ${JSON.stringify(events)}`);console.log('desktop Buffet exact interaction regression passed.')}finally{await page.close()}}
(async()=>{const browser=await (BROWSER_NAME==='webkit'?webkit:chromium).launch({headless:true});try{await runMobile(browser);await runDesktop(browser)}finally{await browser.close()}})().catch(error=>{console.error(error.stack||error);process.exit(1)});
